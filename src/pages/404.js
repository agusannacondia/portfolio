import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => (
  <Layout>
    <div id="main">
      <h1>OOPS!</h1>
      <p>Parece que no hay nada por acá... pero podés volver al <a href="/">inicio.</a></p>
    </div>
  </Layout>
)

export default NotFoundPage
